import {} from "../types";

const initialState = {
  userList: [],
  loading: false,
};
export const productReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
